---
category: Cards
---

# CollectionCard

A collection tile from the horizontal collections track.

The image sits in a fixed-height figure and scales 1.06 on hover over 1.2s. The slow
scale is the point — a fast one reads as a web store rather than a manufacturer.

Name in the mid role, finish and format line underneath in muted 12px.

## Usage

```tsx
import { CollectionCard } from '@orkay/ds';

<CollectionCard name="Statuario" meta="Glossy · 800×1600" src={slab} alt="Veined black marble-look porcelain slab" />
```
